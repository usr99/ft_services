# # config file version
apiVersion: 1

providers:
 - name: 'InfluxDB'
   folder: 'ft_services'
   folderUid: ''
   disableDeletion: true
   updateIntervalSeconds: 10
   allowUiUpdates: true
   options:
     path: /var/lib/grafana/dashboards
